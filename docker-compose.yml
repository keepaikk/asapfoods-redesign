services:
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    environment:
      - PORT=3001
      - NODE_ENV=production
      - DATA_DIR=/app/data
    volumes:
      - backend-data:/app/data
    networks:
      - dokploy-network
    expose:
      - "3001"
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    networks:
      - dokploy-network
    expose:
      - "80"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.jovivafoods.rule=Host(`jovivafoods.com`) || Host(`www.jovivafoods.com`)"
      - "traefik.http.routers.jovivafoods.entrypoints=websecure"
      - "traefik.http.routers.jovivafoods.tls.certResolver=letsencrypt"
      - "traefik.http.services.jovivafoods.loadbalancer.server.port=80"
      - "traefik.http.routers.jovivafoods.service=jovivafoods"
    restart: unless-stopped

volumes:
  backend-data:
    driver: local

networks:
  dokploy-network:
    external: true
